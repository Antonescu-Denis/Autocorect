current goals:

functionality:
	- have automatic/manual autocorect
	- able to separately enable/disable autocomplete/spell checker
	- detection runs, but only starts making suggestions at 3 word length
	- everything lowercase, but suggestions may be shown capitalized/uppercase
	- max allowed length difference from typed word to suggestion
	- may consider keys proximity when making suggestions

autocomplete implementation ideas:
	- trie (prefix tree)
	- frequency-based dictionary

spell checker implementation ideas:
	- edit distance (levenshtein distance)
	- dictionary lookup
	- candidate generation + ranking

libraries:
	- wordfreq https://pypi.org/project/wordfreq/
	- levenshtein https://pypi.org/project/python-Levenshtein/
	- pyqt5

optional:
	- switch between auto/manual detection




bugs:
	- Ctrl+C crashes UI
		- input works fine
		- calling update_suggestions(), even if empty, will crash
	- when auto insert is enabled, space will trigger half, normally or double